services:
  _defaults:
    autowire: true
    public: true

  App\:
    resource: '../src/'

  App\EventListener\ResponseListener:
    tags:
      - { name: 'kernel.event_listener', event: Framework\Event\ResponseEvent }
